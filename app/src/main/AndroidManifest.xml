<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locale_config"
        android:theme="@style/Theme.MuzeiRoma">

        <activity
            android:name="it.fonsolo.muzeiroma.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity di impostazioni lanciabile da dentro Muzei -->
        <activity
            android:name="it.fonsolo.muzeiroma.MuzeiSettingsActivity"
            android:exported="true"
            android:label="Muzei Roma — Impostazioni"
            android:theme="@style/Theme.MuzeiRoma">
            <intent-filter>
                <action android:name="it.fonsolo.muzeiroma.ACTION_SETTINGS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <provider
            android:name="it.fonsolo.muzeiroma.RomaArtProvider"
            android:authorities="it.fonsolo.muzeiroma.provider"
            android:exported="true"
            android:grantUriPermissions="true"
            android:label="@string/source_title"
            android:description="@string/source_description">
            <intent-filter>
                <action android:name="com.google.android.apps.muzei.api.MuzeiArtProvider" />
            </intent-filter>

            <meta-data
                android:name="com.google.android.apps.muzei.api.provider.load_frequency_minutes"
                android:value="180" />

            <!-- Collega l'activity di impostazioni alla fonte Muzei -->
            <meta-data
                android:name="settingsActivity"
                android:value="it.fonsolo.muzeiroma.MuzeiSettingsActivity" />
        </provider>

    </application>

</manifest>
